# name: 2ms Secret Scan

# on:
#   push:
#     branches: [ "main", "master" ]
#   pull_request:
#     branches: [ "main", "master" ]

# jobs:
#   run-2ms:
#     runs-on: ubuntu-latest
#     env:
#       TWO_MS_VERSION: "2.8.1"

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Run 2ms secret scan
#         run: |
#           mkdir -p reports
#           docker run --rm -v "$PWD":/repo checkmarx/2ms:${{ env.TWO_MS_VERSION }} filesystem \
#             --path /repo \
#             --stdout-format json \
#             --log-level info \
#             --ignore-pattern "**/node_modules/**" > reports/2ms-results.json \
#             || echo "2ms completed (exit code indicates secrets found)"

#           echo "Generated reports:"
#           ls -la reports/

#       - name: Upload JSON report
#         uses: actions/upload-artifact@v4
#         with:
#           name: 2ms-scan-results
#           path: reports/2ms-results.json

name: 2ms Secret Scan
on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
jobs:
  run-2ms:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write  # Required for SARIF upload
    env:
      TWO_MS_VERSION: "2.8.1"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Run 2ms secret scan
        run: |
          mkdir -p reports
          # Generate JSON output (this works reliably)
          docker run --rm -v "$PWD":/repo checkmarx/2ms:${{ env.TWO_MS_VERSION }} filesystem \
            --path /repo \
            --stdout-format json \
            --log-level error \
            --ignore-pattern "**/node_modules/**" > reports/2ms-results.json \
            || echo "2ms completed (secrets found)"
          
          # Skip SARIF for now due to output formatting issues
          echo "Skipping SARIF upload - will demo JSON results instead"
            
          echo "Generated reports:"
          ls -la reports/
      # Skip SARIF upload for now - focus on JSON artifacts for demo
      - name: Upload JSON report as artifact
        uses: actions/upload-artifact@v4
        with:
          name: 2ms-scan-results
          path: reports/
