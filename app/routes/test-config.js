// Test configuration for security demo
const config = {
    apiKey: 'sk-test123456789abcdef',  // This will trigger 2MS
    database: process.env.DATABASE_URL,
    debug: true
};
module.exports = config;localStorage